# 匿名请求的会话身份判定

设计记录:billion-context 如何判定一个**匿名**请求(无 `session_id` 头或 body
字段、无 `prompt_cache_key`)属于哪个会话。在 tail-window reattach 移除(#1115)
之后撰写,作为未来参考。实现:`src/prefix-affinity.ts`;行为测试:
`tests/truncation-fork.test.ts`、`tests/prefix-affinity.test.ts`。

## 原则:内容是唯一不可变的锚点

客户端自带的 id 原样采信。按任何客户端可轮换的东西(凭据、中继、协议转换)
划分会话,会在用户继续对话时恰好把状态弄丢(#286 的教训)。对完全不带 id 的
请求,消息内容是双方共享的唯一稳定物,因此亲和性完全由重放的历史计算得出。

由此推出两条推论:

- **环境不进身份。** 系统提示词、工具定义、模型名从不进入哈希。摘要描述的是
  更早环境下发生的历史轮次;把环境算进去,会让所有 harness 轮换 prompt 的
  会话(常见做法)全部断档,机制本身失效。
- **字节精确是承重墙。** kernel 内部的消息 id 是内容哈希且永不复用。这条路径
  上任何"近似匹配"容忍度都会在 decompress 时静默错配,所以匹配永远字节精确。
  唯一的设计自由度是*去哪里找证据*——而不是*要求多少证据*。

## 全前缀匹配 —— 唯一的采纳路径

每条被跟踪的链存储渐进哈希 `h_i = sha256(h_{i-1} || canon(msg_i))`。当来请
求的渐进哈希与某条存储链的**头部**在最长严格前缀上相等时命中(`via:
"prefix"`)。这是可靠的:

- 身份证据是整个共享头部,锚定在第 0 位——别的对话不可能在不持有你真实开场
  轮次的情况下引用你的第 0 位。
- 分叉自愈(fork 语义,#286):客户端一旦编辑或分叉历史,下一次请求即停止命
  中并铸造新会话,旧链继续服务未编辑的那条线。无需显式失效。
- 安全性:命中必须持有字节一致的头部,因此折叠状态不会泄露请求者尚未持有的
  任何东西。

## Tail-window reattach —— 历史与移除(#316 → #1072 → #1115)

### 它解决的问题

截断重放历史(丢最旧、滚动窗口)的客户端不再能从头部匹配。没有 reattach,它
会在首次截断后的每一轮都铸造新会话,压缩阶梯永远建不起来。观察到的目标类
型:dsh 风格 web 客户端保留 20–30 条的滚动窗口。

### 被移除的机制

解析器把来请求的前导 `w = min(8, depth)` 个逐条哈希,对每条被跟踪链的**任意
链中偏移**做滑动比对,恰好一处连续命中即采纳该存储会话(`via:
"tail-window"`)。

### 为什么它在结构上脆弱

链中连续命中是**对称证据**,无法区分:

- "同一对话,截断重放"(预期场景),与
- "另一段对话引用了共享内容"——模板化工具输出、标准文件内容、常见报错文案。

第二种情形不需要攻击者:并行处理同一代码库的多个 agent 会自然产生相同的链中
切片(被动模板碰撞)。而且无论哪种情形,采纳都是双向有害的:

- 采纳方重建的 wire 会收到受害会话的折叠摘要块(跨对话信息泄露);
- 采纳方的新轮次被记入受害会话(对真正主人的上下文污染)。

临时缓解(#1072)把窗口从 3 提高到完整 8 条,关掉了 sub-8 构造窗口——但更大
的阈值并不打破对称性:持有任意 8 条连续链中切片仍然足够。

### 移除前的测量

本 daemon 部署 3 天生产日志(2026-09-19 → 2026-09-22),约 1.15 万次匿名解析:

| 结果 | 次数 |
|---|---|
| 全前缀命中 | 9,367 |
| 新会话 | 2,106 |
| 记录 fork lineage | 142 |
| **tail-window reattach** | **0** |
| 多候选歧义尾部命中 | 0 |

零使用——连近似命中都没有。

### 决定:彻底移除链中采纳(#1115 方向 3)

截断重放现在 fork 出新会话(`via: "new"`)。接受的成本:一次原始重发加阶梯重
启——与指令漂移 fork 已接受的同一成本级别(#1106)。两个缓解限定损失:

- **可选块采纳(#629)** 携带源消息在来请求中仍完整存在的压缩块(raw-id 集合
  语义,而非前缀血统)。
- **kernel 协调保持可靠**:截断重放优雅地停用受影响块,完整重放使其复活
  (`tests/truncation-fork.test.ts` 固化)。

截断扫描仅以归因形式保留:新会话若其前导 `TRUNCATION_LINEAGE_WINDOW = 8` 条
连续运行严格位于(offset ≥ 1)某条被跟踪链内部,记录 `lineage: { reason:
"truncated", parents }`;若与某链共享前导前缀(LCP ≥ `MIN_FORK_PREFIX = 3`)则
记录 `reason: "forked"` 并优先。lineage 只用于日志/UI/调试,**永不参与匹
配**。

## 被否决的替代方案

| 方向 | 否决理由 |
|---|---|
| 后缀锚定匹配(命中必须结束于存储链尾部) | 仅在客户端恰好保留窗口长度(K = 8)时成立。文档记载的滚动窗口保留 20–30 条——主流合法场景直接断裂。 |
| 深度缩放证据(窗口须覆盖存储链 ≥ 50%) | 固定 8 条窗口无法覆盖超过 16 条深度的链的一半——杀掉长对话,恰恰是最值得保留的那些。 |
| kernel 校验采纳(对照完整 kernel id 链交叉验证) | kernel id 是内容哈希:同样的对称证据,只是延伸到更大深度。精度更高,仍非所有权证明;还给匿名热路径增加一次会话查找。 |
| 尾部锚定最长运行 + 下限(dir 1+ 变体) | 可靠且保留功能。因实测零使用而*暂不*采用;待客户端侧身份信号出现后一并重新考虑,不先上投机性接口。 |

## 三层身份框架

变更下的连续性分解为三个独立层。真值单元是**块**而非会话——连续性天生是部
分的:

1. **附着(Attach)**——请求属于哪条存储链。证据来自客户端可证明持有的东西;
   #1115 之后只有头部锚定的全前缀命中算数。
2. **块有效性(Block validity)**——逐块由 kernel 依据消息 id **存在性**(而非
   位置)判定:被覆盖消息缺失 ⇒ 块停用;它们回来 ⇒ 块复活。"半个会话"是这
   一层的机械结果,不是策略旋钮。
3. **环境(Environment)**——系统提示词/工具/模型不进身份(见原则)。环境变化
   改变的是未来轮次,不是历史摘要的真值。

## 未来方向

持久的修复是**客户端侧身份信号**:由客户端自身提供的稳定 per-conversation
id。在那之前,字节精确的头部锚定匹配仍是安全底线;任何后缀/链中采纳的重新引
入,必须先对照本文档的威胁模型论证。

相关:#1148(OpenAI 内联 `messages[0]` 系统提示词会被计入身份,与其他协议不对
称——单独 issue)。
